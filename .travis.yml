
language: python

python: 3.8.6

install:
    - pip install -r requirements.txt

script:
    - mkdocs build

deploy:
    provider: pages
    skip_cleanup: true
    github_token: $github_token
    local_dir: site
    on:
        branch: master
